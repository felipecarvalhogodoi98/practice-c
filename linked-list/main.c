#include <stdio.h>
#include "list.test.h"
#include "list.test.c"

int main() {
  run_tests();
  // printf("Size: %d\n", size(list));
  // insert(list, 0, 44);
  // push_back(list, 120);
  // push_front(list, 5);
  // push_front(list, 6);
  // push_front(list, 7);
  // int valueat = 2;
  // // printf("Value at %d: %d\n", valueat, value_at(list, valueat));
  // push_back(list, 0);
  // push_back(list, 10);
  // push_back(list, 121);
  // printDebug(list);
  // // printf("Front: %d\n", front(list));
  // // printf("Back: %d\n", back(list));
  // push_back(list, 124);
  // push_back(list, 44);
  // // int n_from_final = 8;
  // // printf("%d index reverse: %d\n", n_from_final, value_n_from_end(list, n_from_final));
  // remove_value(list, 44);
  // printDebug(list);
  return 0;
}